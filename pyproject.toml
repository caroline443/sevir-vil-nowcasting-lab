[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sevir-nowcasting"
version = "0.1.0"
description = "Problem-first SEVIR VIL nowcasting experiments"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
